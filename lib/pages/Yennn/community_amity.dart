import 'package:flutter/material.dart';

class CommunityAmity extends StatelessWidget {
  const CommunityAmity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
